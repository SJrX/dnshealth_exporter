# Authoritative for dmarc-absent.demo. — SPF present (-all) but no
# DMARC record (spec 009). SPF rows PASS; the DMARC-present row WARNs
# (absent) and the DMARC-policy row reads N/A.

dmarc-absent.demo. {
    file /zones/dmarc-absent.demo.zone
    log
    errors
}
