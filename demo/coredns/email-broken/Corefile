# Authoritative for email-broken.demo. — broken email-auth (spec 009):
# TWO v=spf1 records (RFC 7208 §3.2 permanent error) and a DMARC record
# missing the mandatory p= tag. The SPF-valid row and the DMARC-valid row
# both read FAIL.

email-broken.demo. {
    file /zones/email-broken.demo.zone
    log
    errors
}
