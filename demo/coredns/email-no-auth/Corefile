# Authoritative for email-no-auth.demo. — neither SPF nor DMARC (spec 009).
# The zone is reachable and answers, but publishes no email-auth records,
# so the present rows WARN (absent) and the qualifier/policy rows N/A.

email-no-auth.demo. {
    file /zones/email-no-auth.demo.zone
    log
    errors
}
